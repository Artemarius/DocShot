# Copy this file to keystore.properties and fill in your values.
# DO NOT commit keystore.properties to version control.
#
# Generate a keystore:
#   keytool -genkeypair -v -keystore docshot-release.jks -keyalg RSA -keysize 2048 -validity 10000 -alias docshot
#
storeFile=docshot-release.jks
storePassword=
keyAlias=docshot
keyPassword=
